#include <iostream>
 #include <thread>
 #include <mutex>
 #include <ctime>
 #include <windows.h>
 #include <vector>
 #include <bits/stdc++.h>
 #include <cstdlib> 
  		 std::mutex mtx; 
 void worker(){
 	for(int i = 0; i < 10; i++){
	mtx.lock();
 	system("start worker.exe");
	mtx.unlock();
 }
 }
 int main(){
 	system("start worker.exe");
 	system("start worker.exe");
  	system("start worker.exe");
  	
  	std::thread A(worker);
  	std::thread B(worker);
   	std::thread C(worker);
   	
   	A.join();
   	B.join();
   	C.join();
 	return 0;
 	
 }
